# Final results

This directory contains only paper-facing outputs from the active pipeline. Historical and exploratory outputs are stored under `archive/results/`.

Figures are stored in both PNG and PDF where available. CSV tables are the machine-readable source; TeX files are presentation exports.

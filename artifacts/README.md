# Artifacts

`demand/`, `calibration/`, and `policy/` contain compact fitted objects used between active notebooks. `cache/` contains regenerable schedule systems and is ignored by Git.

Python pickle files are environment-sensitive. Their producing notebook, schema assumptions, and canonical consumers are documented in `docs/artifact_registry.md`.

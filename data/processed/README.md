# Processed data

Only stable processed samples belong here. Fitted models and optimization objects belong in `artifacts/`; large regenerable schedule systems belong in `artifacts/cache/`.

Raw Dominick's scanner files are not distributed.
